# =============================================================
# TICKETY - unified backend (web + mobile)
# Rebuilt from the actual imports in the merged source rather than
# a full `pip freeze`. The mobile backend's freeze carried ~40
# unused packages (torch, opencv, whisper, pygame, pymongo,
# psycopg2, matplotlib, ...) that the ticketing code never imports.
# =============================================================

# --- Web framework ---
Flask==3.1.2
flask-cors==6.0.2

# --- ORM + MySQL driver ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.45
PyMySQL==1.1.2          # DATABASE_URL uses mysql+pymysql://...
cryptography==46.0.6    # required by PyMySQL for caching_sha2_password auth

# --- Auth / security ---
bcrypt==5.0.0

# --- Background jobs (carry-over requeue scheduler) ---
APScheduler==3.10.4
tzlocal==5.2

# --- Config + outbound HTTP (Brevo transactional email API) ---
python-dotenv==1.2.1
requests==2.32.5

# --- Push notifications (#8) — optional; only needed if FCM is configured ---
google-auth==2.35.0

# --- Production WSGI server ---
gunicorn==23.0.0